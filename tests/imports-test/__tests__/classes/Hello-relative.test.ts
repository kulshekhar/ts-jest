declare var jest, describe, it, expect;

import { Hello } from '../../src/classes/Hello';

describe('Hello Class relative', () => {
  it('should throw an error on line 11', () => {
    const hello = new Hello();
  });
});
